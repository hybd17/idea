package recent;

import java.util.Scanner;

public class Level3 {
   /* Scanner in = new Scanner(System.in);
    int m = in.nextInt();
    public class functions{
        int m ;//m值由输入学生数决定
        String number;
        String name;
        String[][] students;
        //输入数据(暂时为空)
        void input()
        {
            for(int i = 0;i<students.length;i++)
            {
                for(int k = 0;k<students[i].length;k++)
                {
                    if(students[i][k] == null)
                    {
                        students[i][k] = in.next();
                    }
                    System.out.println(students[m][i]);
                }
            }
        }
        void delete()
        {
            number = in.next();
            for (int i = 0;i<students.length;i++)
            {
                if(students[i][1].equals(number) == true)
                {
                    for(int k = 0;k<=students[i].length;k++)
                    {
                        students[i][k] = null;
                    }
                }
            }
        }
        void change()
        {
            //输入要改的学生姓名与学号
                name = in.next();
                number = in.next();
            boolean isprime = false;
                for(int i = 0;i<students.length;i++)
                {
                    if(students[i][0].equals(name) && students[i][1].equals(number))
                    {
                        students[i][0] = in.next();
                        students[i][1] = in.next();
                        isprime = true;
                        break;
                    }
                }
                if(!isprime)
                {
                    System.out.println("您输入的学生不在名单中");
                }
        }
    }

    public static void main(String[] args)
    {
        functions fc = new functions();
        System.out.println("以下三种功能，请您选择：");
        System.out.println("1.输入");
        System.out.println("2.删除");
        System.out.println("3.改写");
        int select = in.nextInt();
        switch (select) {
            case 1: {
                fc.input();
                break;
            }
            case 2 : {
                fc.delete();
                break;
            }
            case 3 : {
                fc.change();
                break;
            }
        }
    }*/

}
